[Unit]
Description=Aquila AI Backend
After=network.target

[Service]
User=root
WorkingDirectory=/root/aquila-backend
ExecStart=/usr/local/bin/gunicorn -w 2 -b 0.0.0.0:5000 app:app
Restart=always

[Install]
WantedBy=multi-user.target
